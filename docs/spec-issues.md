# Spec issues

Contradictions and gaps in `docs/physics-model.md`, the skills and the scaffold, raised for the spec
owner (roadmap open question 10) rather than silently resolved in code. Each row carries the
resolution the roadmap and the code assume; the **status** column below records what has been applied.
Line numbers refer to `docs/physics-model.md` at commit `3b653b3` unless stated. Opened by roadmap
step M0.10 on 2026-09-10 and maintained thereafter: when a step applies or overturns a resolution,
update the status here in the same commit.

## Status of the coding-blocking fixes (applied in M0.10, awaiting spec-owner review)

| # | change applied to `docs/physics-model.md` | why it blocked coding |
|---|---|---|
| S1 | §5.3(a): `T_sky(θ_el) … cos^q(θ_el)` → `T_sky(θ_zen) … cos^q(θ_zen)` | the sky is coldest at zenith (line 314, R13); the old form put the maximum depression at the horizon, and M6.5 / MS.2 tests encode coldest-at-zenith |
| S2 | §6.1: absorbed solar `(1−α_sol)^c Q_sol` → `α_sol Q_sol` in the displayed balance | the prose two lines below already said so; M6.7's monotone-α test encodes it |
| T4 | §12.2: `spectral_response: "responses/boson_vox.csv"` → `"spectra/responses/boson_vox.csv"` (relative to `data/`, µm, peak-normalised) | the path was stated four ways; M0.8's loader and ADR 0008 fix the layout as `data/spectra/responses/` |

**Where the sixty-nine rows stand** (RP.7, 2026-09-16; nine rows added 2026-09-20). The `status`
column on each row is the ledger; this is its summary. **Thirty-seven** rows are carried by an ADR that
exists, **nineteen** by code with no ADR, and **thirteen** are open: `S9`, `S15` and `S16` are edits to
`docs/physics-model.md` that belong to the spec owner, **`S13` is open again** — see its row — and
`S41`–`S49` are the gaps the 2026-09-18 audit found between the spec and the owner's requirements
(lateral and part-to-part conduction, a solved engine node, the shadow term's provider, latent heat,
participating media, snow, vegetation, people), each owned by a roadmap row in phase P or C.
(`S8` is counted as code: its half of the fix shipped in M9.1 and only the spec's wording is left.)

This paragraph used to read "Everything else is open", which had been wrong for months: it listed
ten M0-era resolutions while fifty-six of the sixty rows had shipped. The status column exists so that
the claim is per-row and checkable rather than a sentence somebody has to remember to edit.
`tests/unit/test_spec_issue_status.py` enforces the mechanical half — every ADR named in a status
cell exists, and no row may be `open` while the ADR its resolution names is already written.

**Physics (fix the spec):**

| # | issue | proposed resolution | status |
|---|---|---|---|
| S1 | §5.3(a) line 311: `T_sky = T_air − ΔT_clear(1−cloud)cos^q(θ_el)` puts zero depression at zenith and maximum at the horizon — inverted vs line 314 ("at zenith"), line 305 (`L_sky(θ_zen)`) and R13 | Change to `cos^q(θ_zen)`; calibrate (ΔT_clear, q) per band to the digitised R13 profiles (the quoted 55–70 K, q 0.5–1 do not reproduce −40 °C at 15° elevation) — M6.5, MS.1 | code — M6.5, MS.1 |
| S2 | §6.1 line 355 writes absorbed solar as `(1−α_sol)^c Q_sol` (stray exponent) while line 363, §6.4 and the skill use `α_sol Q_sol` | Use `α_sol Q_sol`; fix the displayed equation — M6.4 | code — M6.4 |
| S3 | §6.4 line 412: RK2 stability "60–200 s for thin painted metal" ignores the 1/R12 term; with §16.2 car paint and 1.2 mm the bound is 0.24 s | ADR 0036: thin panels are single nodes with a resistive back boundary; keep 1–60 s ticks — M6.8 | ADR 0036 |
| S4 | §6.4 uses R2d, T_deep, δ1, δ2 but defines none; §12.3 has one `thickness_m` | Define in ADR 0036 (R2d = δ2/(2k) + δ_deep/k; T_deep = spin-up-window mean of T_air; δ2 per material default) | ADR 0036 |
| S5 | §7.3 line 495 calls `w` "precipitable water (g m⁻³)"; the formula gives absolute humidity; RH unit (fraction vs percent) unstated | Rename to absolute humidity; RH is a fraction asserted at the loader — M8.2, M6.1 | code — M8.2 |
| S6 | §7.2 haze row: visible τ 0.45–0.65 at 200 m contradicts Koschmieder at 5 km visibility (0.855); implies V ≈ 1–1.8 km | Keep Koschmieder and the visibility label; re-derive the row — M8.4 (ADR 0049) | ADR 0049 |
| S7 | §9.4 line 661 quotes the paraxial 4F² datasheet NETD two paragraphs before the +1 form; §8.1 line 525 says "NETD scales as F²" | Implement only the +1 form; keep 4F² as a labelled datasheet conversion (ratio 1.25 at F/1) — M4.5 (ADR 0024) | ADR 0024 |
| S8 | §9.2 line 634 "smears over roughly 0.6 frames" is τ_th/Δt, not the smear extent (the IIR reaches 81 % in one frame); same phrase in the skill | Reword; tests use the closed form only — M9.1 | code — M9.1 · spec reword open |
| S9 | §9.2 line 629 uses R₀ for both responsivity (V/W) and bolometer resistance (Ω) | Rename one symbol | open — spec owner |
| S10 | §13.4 SPG#4 emits `DN_ideal` and SPG#5 adds noise after quantisation, contradicting §9.1 (n_e before floor) and CLAUDE.md #3 for shot noise | Noise before quantisation; quantise last — ADR 0026 | ADR 0026 |
| S11 | §4.2 line 236 water n/k gives ε(0) = 0.990, ε_hemi = 0.951; §16.2 says 0.96. Bulk Al n/k gives ε ≈ 0.012 vs table 0.09. The table never says what it tabulates | ADR 0043 defines the convention and whether an effective k is fitted — M7.8/M7.9 | ADR 0043 |
| S12 | §4.1 defines ρ as the hemispherical DHR; §4.2 uses the specular Fresnel R as 1−ε — exact only for smooth surfaces, while §4.3 requires roughness for the same materials | ADR 0042: specular Fresnel for Level A (smooth) only; Level B/C for rough; error band recorded | ADR 0042 |
| S13 | §12.1 says glass is "opaque-ish" in MWIR; §4.4 gives only LWIR/SWIR; τ_mwir undefined for the headline material. **Reopened (RP.7).** The proposed resolution cannot be carried out: `data/nk/glass.csv` is **fused silica** standing in for soda-lime, and by design it supplies the angular *shape* while `emissivity_per_band` supplies the magnitude (M7.5). Beer–Lambert over the authored 5 mm of that k(λ) gives nir 0.935 / swir 0.936 / mwir 0.109 / lwir 0.000 against the authored 0.77 / 0.70 / 0.02 / 0.0 — fused silica lacks the iron and alkali that make real windscreen glass absorb in the near infrared, so the proxy is transparent where soda-lime is not. Only LWIR agrees, where the Si–O reststrahlen band makes both opaque. `mwir: 0.02` was authored in `412f019`, six commits before the table existed (`1a0f13c`), so the table never was its source | **Not** Beer–Lambert over the proxy. The value is declared `transmittance_derivation: "authored: S13"` in the YAML so it cannot be mistaken for a derived number, and `tests/unit/test_transmittance_derivation.py` holds that declaration against the recomputation. Closing this needs a soda-lime n/k source or a measured τ_mwir — M7.9 is not sufficient | **open** (reopened RP.7) |
| S14 | §3.2(b) LUT 200–1000 K vs §5.3 cold sky below 200 K (T_air < 270 K) and §5.4 glint above 1000 K; the §13.5 kernel clamps silently | L_sky by direct quadrature (MS.1); inverse flags out-of-range; range widening evaluated in ADR 0011 | ADR 0011 |
| S15 | §1 line 55 says L3 reflection is "path-traced in T-domain"; §5.3(c) and §13.7 option 3 say radiance domain — temperatures do not add | Fix §1 to "radiance domain" | open — spec owner |
| S16 | §2 line 86 reflected term `(1−ε) L↓` is the opaque-diffuse special case; §4.4 needs `ρ = 1−ε−τ`, §5.4 a specular lobe | Note in §2 that it is the L2 opaque-diffuse form | open — spec owner |
| S17 | §5.2 "compile-time flag" for illumination culling vs §12.2 line 900 "no core code changes" | Runtime switch on `regime` — ADR 0063 | ADR 0063 |
| S18 | §13.5 hard-codes p = 4 with a float2 (ε0, a) table; §4.2 fits p ∈ 4–6 per class; §12.3 has no (a, p) fields | Table carries (ε0, a, p) — ADR 0042 | ADR 0042 |
| S19 | §13.3/§13.7 equate the AO AOV with V_s; §5.3(a) defines V_s as cosine-weighted sky visibility (wall: AO ≈ 1, V_s = 0.5) | V_s = occlusion·(1+n·up)/2 — ADR 0045 | ADR 0045 |
| S20 | §13.3 lists MotionVectors for "bolometer smear"; §9.2 defines smear as an inter-frame IIR | IIR from state; motion vectors feed MTF_motion for photon detectors only — ADR 0059 | ADR 0059 |
| S21 | §8.3 worked MTF_det(ξ_N) ≈ 0.64 assumes w = pitch; config fill factor 0.90 gives 0.669; the spec never says which | Box width = pitch, fill factor in A_d — ADR 0020 | ADR 0020 |
| S22 | §8.3 lists MTF_aberr, MTF_defocus, MTF_elec separately, defines one Gaussian and no MTF_elec | One Gaussian, MTF_elec = 1 — ADR 0059 | ADR 0059 |
| S23 | §11.1 pipeline omits DDE; the skill puts DDE after gamma; §13.4 before gamma; §11.1 names an undefined "temporal filter" | Order AGC → gamma → DDE → polarity → palette; no ISP temporal filter — ADRs 0031, 0058 | ADR 0031, 0058 |
| S24 | Four descriptions of post-NUC drift (§8.2, §9.2, §10.3, §11.2) with no budget; §8.2 both mandates and disparages the random walk | ADR 0053/0054: one budget; pixel-level OU drift only; global drift from self-emission | ADR 0053, 0054 |
| S25 | §10.2 "ratios in NETD units" (σ_TVH = NETD) vs §9.4 NETD from σ_total — 6 % apart for the Boson ratios; 5 % recovery for σ_T = 0.02 is statistically unattainable at 200 frames | ADR 0025 and 0023 | ADR 0025, 0023 |
| S26 | §11.3 plateau P is a per-bin count; §12.2 gives 0.012 dimensionless; "plateau = 0 behaves like linear" is degenerate | P = plateau·N_pixels; P→0 limit defined — ADR 0028 | ADR 0028 |
| S27 | §11.4 palettes include Arctic and omit gray; §12.2 the reverse | Support both sets — M5.4 | code — M0.7 |
| S28 | §7.1 says path radiance varies with elevation; the model has none; sky pixels have no defined owner | Uniform T_air on the path; sky pixels handed to the sky model — ADR 0050 | ADR 0050 |
| S29 | §5.4 `τ_atm^sun` (slant path) has no provider; §7 is horizontal only | ADR 0051/0064 — M8.8, M11.3 | ADR 0051, 0064 |
| S30 | §5.5 airglow in nW/cm² without saying in-band or total; no shape file | In-band definition, OH-Meinel shape with source — ADR 0065 | ADR 0065 |
| S31 | §9.1 cold-shield efficiency has no formula and lives under `optics:` while described as detector physics | **Resolved (M11.5, ADR 0066).** Solid-angle model Ω_admit = min(π, Ω_lens/η_cs), background Φ_bg = A_d(Ω_admit − Ω_lens)L_B(T_housing), exactly 0 at η_cs = 1 and closing the isothermal-enclosure identity A_d·π·L_B at η_cs = 0. The formula lives in `irsim/detector/cold_shield.py` because it is detector physics; the schema field stays under `optics:` where §12.2 puts it, since moving it would break every existing config for a filing-cabinet improvement | ADR 0066 |
| S32 | §16.4 calls steps 1–5 "a defensible LWIR camera" although §5.3 says reflections are not to be skipped | Roadmap keeps the §16.4 order but M7 is part of the first *deployable* release | code — roadmap |
| S33 | §5.3 models the sky by a cloud *fraction* only; for aerial targets clouds are the dominant clutter and no spatial cloud model exists anywhere in the spec | Author one and record it as an approximation (MS.3, ADR 0070); bound it by display-domain clutter statistics (ME.4) | ADR 0070 |
| S34 | §7.4 and Appendix A #2 bound the band-averaged atmosphere to ~500 m; ground-to-air sky targets sit at 0.5–5 km on slant paths, where a single grey γ_B is wrong by an order of magnitude for structured bands and the §5.3 sky formula disagrees with §7's own column emission | Exponential-sum band model with the sky derived from the same layered column (MS.1, ADR 0071); the cos^q sky kept only as a fitted fast path | ADR 0071 |
| S35 | The spec's radiometry is extended-source throughout (§2, §8.1); unresolved targets — the sky-target case — have no point-source treatment beyond §8.3's aliasing remark | Analytic injection below one native pixel in the fill-fraction form that reuses the §2 aperture factor (MS.6) | code — MS.6 |
| S36 | §15 Tier 4/5 assume owned hardware and ground-scene datasets; the data actually available for sky targets is 8-bit post-AGC anti-UAV video | Compare on the display output with matched AGC; Tier 2 benches remain self-consistency (ADR 0003) | ADR 0003 |
| S37 | CLAUDE.md #4 "author one of the three and derive the others" is unsatisfiable for τ > 0 materials (one closure equation, three unknowns) | Read as "author exactly one of ε, ρ, plus τ where it transmits; derive the rest" (ADR 0040, open question 12) | ADR 0040 |
| S38 | §5.5 states that "at full moon, moonlight and airglow radiation densities are comparable" [R7]. Built from standard lunar photometry (m_sun −26.74, m_moon −12.74 ⇒ a flux ratio of 3.98e5) and an OH-Meinel band model, the full moon comes out **12.6x** the §5.5 *default* airglow level over 0.9–1.7 µm — an order of magnitude apart | Recorded, not tuned. The claim is close at §5.5's **upper** airglow value (39 nW/cm² ⇒ 3.2x) and closer still in **spectral density** at the OH band peaks (2–5x), which is a defensible reading of "radiation densities". What the models *do* reproduce is the SWIR rule of thumb that airglow ≈ a quarter moon (1.15x), and that is what the test asserts. ADR 0065; revisit against a measured near-infrared sky spectrum | ADR 0065 |
| S39 | §6.4 gives the two-node stability bound as Δt < 2C₁/(h + 4εσT³ + 1/R₁₂) — correctly — and then says it "lands around 60–200 s" for thin painted metal. Evaluated on §16.2's own car-paint row with §6.4's own R₁₂ definition, **1/R₁₂ is 37 500 W m⁻² K⁻¹** against h + 4εσT³ ≈ 43, and the bound is **0.235 s** — 400× smaller. The quoted range is the *single-node* bound at high wind, i.e. the same formula with the term that dominates it removed | Computed from the equations rather than quoted, and enforced in the solver's constructor so the discrepancy cannot be absorbed silently. The consequence is a design decision, not a smaller step: a thin panel is a **single node with a resistive back boundary** (bound 264 s), and the two-node solver is for surfaces with real depth, where it is comfortable anyway (asphalt 3 456 s, concrete 7 067 s). ADR 0036 | ADR 0036 |
| S40 | §4.2 quotes `a ≈ 0.15–0.35, p ≈ 4–6` for painted metals and plastics, and in the same paragraph instructs "fit (a, p) once per material class against Level A and bake it". Doing that against a measured acrylic (PMMA, `data/nk/paint_proxy.csv`) gives **a = 0.73–0.77 with p = 4** in every band — 2–3× the quoted range. The quoted values are not a mild approximation of the fit: they hold ε at 0.97 of normal at 70° where Fresnel gives 0.86, which is 3.8 K of apparent temperature against a 250 K sky, and 8.7 K at 80° | Follow the instruction, not the numbers: the library's four painted materials carry the fitted (a = 0.75, p = 4) from M7.5's proxy. §4.3 supports this — a clearcoat is optically smooth at 10 µm (σ ≪ λ/8), which is the regime specular Fresnel describes. The quoted range may be remembered from *matte* or heavily pigmented coatings; if so §4.2 should say which surface finish it means, since the library contains both. Note p = 4 also sits at the **edge** of the sanctioned candidate set {4, 5, 6} and p = 3.5 fits 2.3× better, which suggests the quoted p range is a touch high too — left alone, inside the spec — M7.5, recorded in ADR 0042 (RP.4) | ADR 0042 |
| S41 | §6.1/§6.4 carry only through-thickness conduction; Appendix A #1 declares lateral flow out of scope, yet the owner's sunlit/shaded step across one metal panel spreads over the skin's diffusion length (aluminium 270 mm, steel 99 mm over 750 s, against 71–150 mm cells) | Add §6.7: in-plane conduction on the patch grid, implicit at the thermal tick, k → 0 recovering §6.4; amend Appendix A #1 when it ships — `PT.11`, `TC.1` | open — `PT.11` |
| S42 | §6.4/§6.6/§12.3 give no way to connect two solved nodes; a hot engine warms the bonnet only by radiation onto a parallel panel and never the mounts, brackets or wings it is bolted to (the owner's R2, 2026-09-18) | Add §6.8: a thermal-link network — nodes with capacity, links as h_c·A or G [W/K], a bay-air fluid node, imposed heat in watts — solved with the fields; schema `nodes:`/`links:` and a joint-conductance table — `TC.2`, `TC.4` | open — `TC.2` |
| S43 | §6.6 prescribes ΔT_max and τ per source; a scripted rise cannot depend on ambient, airflow, load history or the metal attached to it, and the hot soak after key-off (skins peak 1–2 min later in every under-hood measurement) is unrepresentable | Solve heat-source nodes from imposed heat in watts with thermal mass and the S42 network; §6.6's ranges become acceptance bands — `TC.5` | open — `TC.5` |
| S44 | §5.4 and §6.1 use a shadow term S with no provider; §13.3 lists no shadow plane; the render path sets S = 1 on every frame and PT.1's occluders are hand-placed rectangles no scene declares; the sky view factor is the unoccluded form | Define S as a per-cell visibility query against scene geometry (rectangles first, meshes after, neighbours included) shared by the thermal balance and the solar term, with the 0.53° penumbra; occluders in §12.3; a per-cell sky view factor scaling diffuse solar and longwave — `PT.18`, `PT.21`, `PT.22` | open — `PT.18` |
| S45 | §6.1 has no latent-heat term; §15 Tier 3 asks that "wet asphalt reads colder than dry" emerge unscripted, which the balance cannot produce; §16.2's water is a material only and `sea_skin.py` omits the latent flux by its own admission; nothing consumes `precip_mm_h` | Add −L_v·E with a per-cell water film fed from `WeatherSeries.precip_mm_h`, and a mixed-layer water-body node to §6.4 — `PH.1`, `PH.3` | open — `PH.1` |
| S46 | §2 is surface-only; §6.6's plume row and any fire or flame need emission and absorption along the ray, which no term provides; revision 5 deferred the plume without a step past a trigger that had fired | Add §7.5: a per-band gas slab L = τ_b L_behind + (1 − τ_b) B_b(T_g) with κ_b(T) tables per species generated offline, applied at the target's range; MWIR Tier 3 plume test — `PH.4`–`PH.7` | open — `PH.4` |
| S47 | §16.2 snow is a material; a snow surface is a phase-changing state capped at 273.15 K by the latent heat of fusion, and the cap is its LWIR signature | Add a melt cap and melt rate to §6 — `PH.10` | open — `PH.10` |
| S48 | §16.2 vegetation is a leaf material; a canopy is a low-capacity element cooled by transpiration by day, several K below air at midday vapour-pressure deficit, and not at night | A stomatal resistance in the S45 latent term — `PH.11` | open — `PH.11` |
| S49 | §6.6 gives humans a ΔT row; a person is exposed skin near 35.7 − 0.028 (M − W) °C and a clothing surface set by insulation, wind and the radiant environment — two temperatures on one prim | A two-region person object with the ISO 7730 clothing balance — `PH.12` | open — `PH.12` |


**Repository / tooling (fix in M0):**

| # | issue | resolution | status |
|---|---|---|---|
| T1 | `np.trapezoid` needs NumPy ≥ 2.0; `pyproject` allows ≥ 1.24; suite red on 1.x; CHANGELOG claims green | M0.1 | code — M0.1 |
| T2 | `make golden-update` passes an unregistered option; `tests/golden` never runs in `make check` | M0.2 | code — M0.2 |
| T3 | CLAUDE.md #2 and #4 claim tests that do not exist | M0.5 (encode/decode), M7.2 (`test_committed_library_closes_in_every_band`, the library walk) | code — M0.5, M7.2 |
| T4 | Spectral-response path stated four ways (`responses/`, `spectra/`, `data/spectra/`, skill) and the file is missing | M0.10 fixes the spec to `data/spectra/responses/`; M0.8 resolves it; M1.3 authors the file | code — M0.8, M1.3 |
| T5 | `.gitignore` ignores `data/lut/*` while the ir-radiometry skill says LUTs are committed | ADR 0012 | ADR 0012 |
| T6 | Stefan–Boltzmann tolerance: spec 1e-6, skill 1e-12, test 2e-5 with no ADR | M0.4 (ADR 0005) | ADR 0005 |
| T7 | `tests/conftest.py` fixtures are two of the five the testing skill requires; `material_id = 0` will collide with the UNMAPPED sentinel | M0.6, M7.10 | code — M0.6, M7.10 |
| T8 | `test_layering.py` forbids `carb` (CLAUDE.md does not) and neither forbids `irsim → irsim_isaac` | M0.3 | code — M0.3 |
| T9 | CLAUDE.md calls `irsim_isaac/pipeline` the "Warp-based reference pipeline" while both skills make the NumPy path the oracle; no `irsim/pipeline`, `irsim/validation`, `irsim/io` or `irsim_eval` in the layout; scopes `pipeline`/`validation`/`eval`/`io` not in the commit-scope list | ADR 0018; the CLAUDE.md changes are bundled into open question 12 (the user asked that CLAUDE.md not be edited by this plan) | ADR 0018 |
| T10 | `generate_luts.py` plans three tables; §9.4 needs the photon-form derivative (`dLb_q_dT`) too; `planck.py` lacks it | M1.1, M1.6 | code — M1.1, M1.6 |
| T11 | Makefile `luts` passes no data root; `mypy --strict` will fail on `import yaml` (no stubs) | M1.9, M0.7 | code — M0.7, M1.9 |
| T12 | Boson YAML: 14 mm/32° HFOV inconsistent (30.7°); `spectra/boson_vox.csv` missing; photon `null` keys omitted | M0.7 test documents it; M1.3; schema accepts both forms | code — M0.7, M1.3 |
| T13 | ir-radiometry skill 0.1 % top-hat check ≠ the 5 mK LUT check (0.1 % ≈ 63 mK at 300 K); the ship-step example claims "< 2 mK" from a 0.1 % comparison | Both tests exist separately and print mK — M1.4, M1.6 | code — M1.4, M1.6 |
| T14 | No config field declares a camera non-radiometric; `outputs.apparent_temperature` is the proxy | ADR 0021 | ADR 0021 |
| T15 | `conftest.py` says every random test must take `rng`; the noise skill seeds from (frame, sensor) | Physics tests seed via the stage API; `rng` for ad-hoc sampling — M4.2 | code — M4.2 |
| T16 | CLAUDE.md, README, §13 and the `isaac-sim-spg` skill say "Isaac Sim 6.0"; the build on the development machine is **6.1.0-rc.26** (Kit 110.3.0, `omni.rtx.spg` 0.4.0, `isaacsim.sensors.experimental.rtx` 1.9.0, Warp 1.16.0 bundled as a Kit extension) and every engine fact in ADR 0014 was measured there | ADR 0014 pins the measured build; README says 6.x and points at the ADR; the CLAUDE.md wording joins open question 12 (CLAUDE.md is not edited by this plan) | ADR 0014 |
| T17 | §13.1/§13.3 and the skill assume a float32 `PtSelfIllumination` (or `HdrColor` at 0 bounces) carrying the encoded temperature. Measured in 6.1: every colour AOV — `HdrColor`, `LdrColor`, all `Pt*` — is **float16** and exposure-scaled (readback ≈ 2.8e-4 × emissive colour × intensity), the float32 `GroundTruthEmission*` AOVs return no data, and path tracing at 0 bounces returns a constant. fp16 caps the round trip near 100 mK at 300 K, ten times the §13.3 bound | ADR 0014: the renderer transports **instance ids + float32 geometry** (`instance_segmentation` exact per pixel, `Camera3dPositionSD`/`DistanceToCameraSD` float32 at full resolution) and the per-facet temperature is looked up in a float32 Warp table — no quantisation at all; the §13.3 table and the skill need rewording by the spec owner; M10.1/M10.2/M10.18 carry the change | ADR 0014 |
| T18 | §7.2 table, haze row: the "5 km vis" label is inconsistent with its own τ columns — Koschmieder at 5 km gives τ_vis(200 m) = 0.855, not 0.45–0.65, and the SWIR column would need an aerosol extinction ratio above 1; the four columns are reproduced at V ≈ 1.5 km with wavelength-ordered ratios | ADR 0049 takes the τ values as authoritative; the `haze` preset is checked at V = 1.5 km | ADR 0049 |
| T19 | §7.3 calls w "precipitable water (g·m⁻³)"; precipitable water is a column amount (mm or kg m⁻²), the quantity in the Magnus expression is the absolute humidity (water-vapour density) | `irsim.atmosphere.humidity` names it `absolute_humidity_g_m3`; the 216.7 factor is derived from R_v (M8.2) | code — M8.2 |
| T20 | §5.3(a) says "under overcast, `T_sky → T_air`"; a cloud radiates at its **base** temperature, which is Γ_env z_LCL colder (6.5 K for a 1 km base, > 100× a Boson NETD). The spec's limit is the saturated-air special case (z_LCL = 0, i.e. fog) | MS.3 / ADR 0070: the overcast limit is `L_B(T_base)`; tests needing a sky at `T_air` use cloud = 1 with RH = 1 | ADR 0070 |

---

